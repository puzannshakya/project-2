export interface WindProps {
    windSpeed: number;
    gustSpeed: number;
    windDirection: number;
    collapseState: Boolean;
}

export interface WindStyleProps {
    windDirection: number;
    collapseState: Boolean;
}
